module.exports = {
    project: {
        ios: {},
        android: {}, // grouped into "project"
    },
    assets: ["./assets/fonts/", "./node_modules/react-native-vector-icons/Fonts"], // stays the same
};